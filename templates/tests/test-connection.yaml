apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ga4gh-reference-cloud-helm.fullname" . }}-test-connection"
  labels:
    {{- include "ga4gh-reference-cloud-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ga4gh-reference-cloud-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
