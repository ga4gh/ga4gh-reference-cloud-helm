apiVersion: batch/v1
kind: Job
metadata:
  name: kratos-db-migration
spec:
  template:
    spec:
      volumes:
        - name: kratos-config-map
          configMap:
            name: kratos-config-map
      containers:
        - name: kratos-db-migration
          image: oryd/kratos:v0.9.0-alpha.2
          volumeMounts:
            - name: kratos-config-map
              mountPath: /etc/config
              readOnly: true
          command: ["kratos", "-c", "/etc/config/kratos.yaml", "migrate", "sql", "-e", "--yes"]
          env:
            - name: DSN
              value: postgres://kratos:secret@host.docker.internal:5432/kratos?sslmode=disable
      restartPolicy: Never
  backoffLimit: 4