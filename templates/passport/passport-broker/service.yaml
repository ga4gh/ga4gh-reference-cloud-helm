apiVersion: v1
kind: Service
metadata:
  name: passport-broker
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: passport-broker
  ports:
    - name: a
      port: 4500
      targetPort: 4500
      protocol: TCP
    - name: b
      port: 4501
      targetPort: 4501
      protocol: TCP
