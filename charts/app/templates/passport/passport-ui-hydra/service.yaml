apiVersion: v1
kind: Service
metadata:
  name: passport-ui-hydra
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: passport-ui-hydra
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
