apiVersion: v1
kind: Service
metadata:
  name: refcloud-api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: refcloud-api
  ports:
    - name: public
      port: 8080
      targetPort: 8080
      protocol: TCP