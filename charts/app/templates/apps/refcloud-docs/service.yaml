apiVersion: v1
kind: Service
metadata:
  name: refcloud-docs
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: refcloud-docs
  ports:
    - name: public
      port: 80
      targetPort: 80
      protocol: TCP
